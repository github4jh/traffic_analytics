# Core service
fastapi>=0.115
uvicorn[standard]>=0.32

# Vision pipeline
opencv-python-headless>=4.10
ultralytics>=8.3

# torch is intentionally NOT pinned here: install the build that
# matches your platform/GPU, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# The Dockerfile installs the CPU build explicitly for the same
# reason -- letting pip pick a default can pull in a large CUDA
# build you don't need on a CPU-only deployment target.
numpy>=1.26
